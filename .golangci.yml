version: "2"
run:
  timeout: 5m
linters:
  default: none

  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - bodyclose

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt